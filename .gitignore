# Upstream sources are downloaded, never committed — this repo ships a patch.
.upstream/
# Go module cache staged for offline builds.
.gomodcache/
# Vendored toolchains for sandboxed executors.
.toolchain/
dist/
*.tar.gz
